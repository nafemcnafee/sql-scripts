-- The Boss  
-- q1.sql
-- List all the data in all tables (Keep the data in a file but do not print for
-- submission.  Only submit the query syntax for this question. Use the data to 
-- verify the correctness of your other queries.)

SELECT * 
  FROM s1.customer;

SELECT * 
  FROM s1.car;

SELECT * 
  FROM s1.saleinv;

SELECT * 
  FROM s1.baseoption;

SELECT *
  FROM s1.invoption;

SELECT *
  FROM s1.servinv;

SELECT *
  FROM s1.servwork;

SELECT *
  FROM s1.prospect;

SELECT *
  FROM s1.employee;
